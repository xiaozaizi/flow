# Admin frontend (Vue 3 + Element Plus skeleton)

This folder holds a minimal skeleton for the admin UI. To run locally, create a normal Vue 3 project and copy build outputs into `src/main/resources/static/admin`.

Key pages to implement:
- tenants, process definitions, process instances, task list, task detail
- Use bpmn-js to render process diagrams and highlight nodes

